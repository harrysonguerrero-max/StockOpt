# En Amplify la interfaz y la API viven en dominios distintos, asi que el front
# necesita la URL absoluta. Es el endpoint de API Gateway que ya existe en la
# cuenta y hace de proxy HTTPS al ALB del servicio ECS.
VITE_API_BASE=https://v1xsa5mlpl.execute-api.us-east-1.amazonaws.com
